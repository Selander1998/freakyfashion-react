import { Spots } from "./subs/Spots";
import { ArticleCards } from "./subs/ArticleCards";

const Landingpage = () => {
	return (
		<div>
			<Spots />
			<ArticleCards />
		</div>
	);
};

export default Landingpage;
